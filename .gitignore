.env
.env.local
node_modules/
.netlify/

# macOS AppleDouble metadata
._*

# Unused Deno lockfile (project uses Node)
deno.lock

# Local dashboard snapshots — never commit
dashboard/dashboard-backup.*
